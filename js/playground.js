function Playground(playground) {
	this.offsetLeft = playground.offsetLeft;
	this.offsetTop = playground.offsetTop;

	this.width = playground.clientWidth;
	this.height = playground.clientHeight;
}